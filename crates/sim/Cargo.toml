[package]
name = "sim"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

# tests/boundary.rs reads this file and asserts these two lines are the whole
# list. Adding a third dependency is a decision, not an edit.
[dependencies]
serde.workspace = true
postcard.workspace = true
